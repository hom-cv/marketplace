[lint.isort]
known-first-party = ["app"]

[lint.per-file-ignores]
# SQLAlchemy Mapped["..."] relationships are forward-refs resolved at runtime;
# the referenced classes are intentionally not imported to avoid circular imports.
"app/models/*.py" = ["F821"]
